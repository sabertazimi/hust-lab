class Variable {
    int _saber;
    bool archer_lancer;
    string _Assassin;
    bool[] cASTer;

    int varTest = (void) => {
        bool truth;
        int saber;

        saber = 233;
        _saber = _saber + 1;
        archer_lancer = !archer_lancer;
        _Assassin = "jinke";

        int i;
        cASTer[0] = 1;
        cASTer[1] = 1;
        for (i = 2; i < 10; i = i + 1) {
            cASTer[i] = cASTer[i-2] + cASTer[i-1];
        }
    };
}
